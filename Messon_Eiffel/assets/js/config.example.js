/* ============================================================
   Plantilla de configuración de Supabase.

   Cómo usarla:
     1. Copia este archivo como "config.js" en la misma carpeta.
     2. Rellena SUPABASE_URL y SUPABASE_ANON_KEY con los valores
        de tu proyecto (Dashboard → Project Settings → API).
     3. "config.js" NO se sube a Git (ver .gitignore) — este
        archivo de ejemplo sí, para que cualquiera que clone el
        repo sepa qué variables necesita.
   ============================================================ */

const SUPABASE_URL = 'https://TU-PROYECTO.supabase.co';
const SUPABASE_ANON_KEY = 'TU_ANON_KEY_AQUI';
